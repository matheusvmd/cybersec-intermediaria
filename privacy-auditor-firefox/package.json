{
  "name": "privacy-auditor-firefox",
  "version": "1.0.0",
  "scripts": {
    "lint": "web-ext lint --source-dir extension",
    "test": "node --test tests/*.test.js",
    "run": "web-ext run --source-dir extension",
    "build": "web-ext build --source-dir extension --artifacts-dir web-ext-artifacts --overwrite-dest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Extensão acadêmica de auditoria de privacidade para Firefox",
  "devDependencies": {
    "web-ext": "^10.7.0"
  }
}
